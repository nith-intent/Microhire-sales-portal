# Base URL of the MicrohireAgentChat API (required when portal and API run on different origins)
# Local dev: copy to `.env` — http://localhost:5216 (MicrohireAgentChat)
# Production build: Vite loads `.env.production` for `npm run build` (see that file in repo)
VITE_API_BASE_URL=http://localhost:5216
